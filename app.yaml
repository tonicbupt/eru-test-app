appname: "erutestapp"
entrypoints:
    showenv:
        cmd: "python show_env.py"
    runanddie:
        cmd: "python runanddie.py"
    algalon:
        cmd: "python algalon.py"
build: "echo 1"
volumes:
    - "/mnt/files"
binds:
    /mnt/mfs:
        bind: "/mnt/files"
        ro: true
